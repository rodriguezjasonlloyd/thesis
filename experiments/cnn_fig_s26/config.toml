name = "CNN model with Figshare Dataset - seed 26"
seed = 26

[data]
root = "data"
k_folds = 5
batch_size = 32
num_workers = 2
max_items_per_class = 0
augmented = false
preprocessing = "none"

[model]
architecture = "base"
pretrained = false
with_fsa = false

[optimizer]
learning_rate = 0.0001
weight_decay = 0.001

[training]
num_epochs = 30
patience = 5
min_delta = 0.0001